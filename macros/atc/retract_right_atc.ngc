o<retract_right_atc> sub

M65 P11 ; Turn off move out carousel, to move it back home

M66 P14 L3 Q5 ;check carousel in position sensor
   O100 if [#5399 LT 0]
      (abort, failed to send right carousel home)
   O100 endif

o<retract_right_atc> endsub [1]

M2
